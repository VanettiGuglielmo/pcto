#include <stdio.h>
#include <math.h>

int main()
{
    int x = 2;
    int y = 4;
    int potenza;
    int radice;

    potenza = pow( x, y);
    radice = sqrt(y);
    printf("la potenza di %d è %%d", x, y, potenza);
    printf("la radice quadrata di %d è ")


}