#include <stdio.h>

int main()

{

    int numero;
    
    printf("utente, inserisci un numero");
    scanf("Ad", &numero);

    int divisore = nemero - 1
    while (divisore)
    {
        if (numero% divisore ==0)
        (
            printf("non è un numero primo\n");
        )
        divisore--;
    }

    printf("è un numero peimo\n")

